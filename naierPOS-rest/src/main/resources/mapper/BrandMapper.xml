<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.joker.common.mapper.BrandMapper">
     
    
    <resultMap type="brand" id="brand">
		<result property="id" column="ID" />
		<result property="client.id" column="Client" />
		<result property="name" column="Name" />
		<result property="decimal" column="Decimal" />
		<result property="status" column="Status" />
		<result property="creator" column="Creator" />
		<result property="created" column="Created" />
		<result property="editor" column="Editor" />
		<result property="modified" column="Modified" />
	</resultMap>
    
	<select id="getBrandByID" parameterType="string" resultMap="brand">
		SELECT 
			a.ID,a.Client,a.Decimal,a.Name,a.Status,a.Creator,a.Created,a.Editor,a.Modified
		FROM Brand a
		WHERE a.ID=#{id} 
	</select>
	
	<select id="getBrandByClient" parameterType="string" resultMap="brand">
		SELECT 
			a.ID,a.Client,a.Decimal,a.Name,a.Status,a.Creator,a.Created,a.Editor,a.Modified
		FROM Brand a 
		WHERE 
			a.Status= 1 and a.Client=#{clientId}
			order by a.ID desc
			<if test="start != null and limit != null">
				limit #{start},#{limit}
			</if>
	</select>
	
	<select id="getBrandCountByClient" parameterType="string" resultType="int">
		SELECT count(1)
		FROM Brand a WHERE 
			a.Status= 1 and a.Client=#{clientId}
	</select>
	
	<delete id="deleteBrandByID" parameterType="string">
		DELETE FROM Brand where ID =#{id}
	</delete>
	
	<insert id="insertBrand" parameterType="brand">
		insert into Brand <include refid="brandSet" />
	</insert>
	
	<update id="updateBrand" parameterType="brand" >
		UPDATE Brand <include refid="brandSet" /> WHERE ID = #{id}
	</update>
    
    <sql id="brandSet">
		<set >
			<if test= "id != null">ID=#{id,jdbcType=VARCHAR},</if >
			<if test= "client != null">Client=#{client.id,jdbcType=VARCHAR},</if >
			<if test= "decimal != null">Decimal=#{decimal,jdbcType=VARCHAR},</if >
			<if test= "name != null">Name=#{name,jdbcType=VARCHAR},</if >
			<if test= "status != null">Status=#{status},</if >
			<if test= "creator != null">Creator=#{creator},</if >
			<if test= "created != null">Created=#{created},</if >
			<if test= "editor != null">Editor=#{editor,jdbcType=VARCHAR},</if >
			<if test= "modified != null">Modified=#{modified},</if >
		</set>
	</sql >
    
</mapper>